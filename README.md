# 🔐 File Encryption Tool

This is a **professional file encryption utility** that converts any file into hexadecimal format before encrypting it. When encrypting **large files**, it uses **more RAM and CPU**, providing strong security at the cost of higher system resources.

> Warning: The process is irreversible without the correct key. Always keep backups of your files and keys.

---

## ✨ Features

- Converts any file to **hex** before encryption
- Uses **unique keys** per file
- Fully **offline operation**
- **Guided command-line interface** for easy usage
- Strong encryption optimized for security over speed
- Large files use additional system resources for better protection

---

## ⚙️ How It Works

1. Copy the file you want to encrypt into the **`file_to_hex`** directory
2. Open the directory in **Command Prompt (CMD)**
3. Convert the file to hex (the tool will provide commands or instructions)
4. Copy the resulting hex file into **`Text_file_encryption - Copy`** folder
5. Remove the `.txt` extension from the hex file
6. Run the encryption executable and **follow the guided instructions**
7. Encryption complete ✅

### Decryption

Reverse the process:
1. Move the encrypted file back into the appropriate folder
2. Run the decryption executable
3. Follow the instructions to convert back from hex to original file format
4. Done ✅

---

## 🔑 Important Notes

- The key file is mandatory for decryption
- You can use the same key for multiple files, or different keys per file
- Losing the key or modifying it **means the encrypted file is permanently lost**
- The author is **not responsible** for lost or corrupted data; use at your own risk

---

## 📜 License

- Free to use for personal and educational purposes
- **No modification, redistribution, or commercial use** without author authorization

